# MongoDB
 In web applications, MongoDB is connected using  libraries like Mongoose (for Node.js), which helps in managing data easily. The connection between frontend (HTML  form) and database is done through a backend server (Express.js). When a user submits data, it is sent to the server,  processed, and stored in MongoDB Atlas (cloud database). 
